{
    auto_https off
    admin off
}

:8082 {
    # 前端静态文件
    root * /usr/share/caddy/html
    file_server

    # 前端路由支持（SPA fallback）
    handle {
        try_files {path} /index.html
        file_server
    }
}
